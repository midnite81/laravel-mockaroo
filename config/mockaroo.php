<?php
return [

        /*
        |--------------------------------------------------------------------------
        | Table Prefix
        |--------------------------------------------------------------------------
        | Although it's expected that this package will be installed into a fresh
        | instance of Laravel, you can prefix the tables so that they don't
        | conflict with any existing tables. By default the prefix is empty.
        |
        */
        'prefix' => '',


];